const TypeScript = () => {
  const user = {
    name: "Ricardo",
    age: 36,
  };
  return (
    <div>
      <p>
        {user.name} | {user.age}
      </p>
    </div>
  );
};

export default TypeScript;
